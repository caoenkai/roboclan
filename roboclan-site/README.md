# Roboclan — live site (v1, static)

The approved Roboclan prototype, ready to deploy on Vercel.
- index.html — the whole site (self-contained)
- favicon.png / apple-touch-icon.png — browser tab + mobile icon

Deploy: push to GitHub → import on Vercel (framework: Other, no build) → add domain roboclan.ai.
Next iteration wires up Supabase + an admin panel so robots can be edited without code.
